docker run -d \
  --name mysql \
  --network app-network \
  --restart unless-stopped \
  -e MYSQL_ROOT_PASSWORD=rootpassword \
  -e MYSQL_DATABASE=university_lab \
  -e MYSQL_USER=user \
  -e MYSQL_PASSWORD=password \
  -v mysql_data:/var/lib/mysql \
  -v /opt/mywebsite/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql \
  mysql:8
